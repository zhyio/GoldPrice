# GoldPrice 全功能测试报告

## 1. 测试结论

测试日期：2026-06-11  
最终完成时间：2026-06-11 15:27 CST（UTC+8）  
基线提交：`68a2e61`  
结论：**通过**

本轮完成源码审查、README 更新、缺陷修复、自动化测试扩充、实时接口检查、
Release 构建、App bundle 验签和隔离数据目录下的完整 UI 验收。

- 自动化测试：62 项，全部通过。
- 核心模型、服务、存储和持仓业务代码行覆盖率：98.44%。
- Release 构建、App 打包、Info.plist 校验和严格代码签名验证：全部通过。
- 实时行情及基金接口：可用；`027300` 当前无估值，应用按预期显示 `--`。
- UI 功能：行情、展开收起、窗口拖动、链接、增删调仓、持久化、错误提示、
  损坏恢复、动态高度和长列表滚动全部通过。
- 真实持仓文件测试前后 SHA-256 均为
  `0d0de305fd433bcf91491e7600e5c15565f6fcaf9fd0b75974f80b81d41ac056`。
- 测试完成后已删除临时 QA 目录，清除 `GOLDPRICE_DATA_DIR`，并重新启动生产实例。

## 2. 测试环境

| 项目 | 值 |
| --- | --- |
| 系统 | macOS 26.5.1（Build 25F80） |
| 架构 | arm64 |
| Swift | Apple Swift 6.2.3 |
| Package tools version | Swift 6.0 |
| 最低部署版本 | macOS 14.0 |
| 测试框架 | swift-testing 6.2.3 |
| 数据隔离 | `GOLDPRICE_DATA_DIR=/tmp/goldprice-qa.*` |

## 3. 自动化测试

执行命令：

```bash
swift test --enable-code-coverage
```

结果：7 个测试套件、62 项测试全部通过。

| 测试域 | 主要覆盖内容 | 结果 |
| --- | --- | --- |
| 行情模型与解析 | 涨跌趋势、格式化、精度边界、缺失字段、非法基准价和非正价格 | 通过 |
| 行情服务 | 双源成功、部分失败、全部失败、旧值保留、请求头、2xx/非 2xx 和网络错误 | 通过 |
| 基金模型 | 收益、今日盈亏、格式化、缺失净值、Codable 持久化字段 | 通过 |
| 基金服务 | JSONP、可选字段、代码不匹配、无估值、并发返回、请求形状和 HTTP 错误 | 通过 |
| 持仓业务 | 添加、代码/金额校验、重复、加仓、减仓、超额减仓、无净值和删除 | 通过 |
| 持仓存储 | 隔离目录、缺失文件、读写往返、原子写入、损坏备份恢复和错误报告 | 通过 |

覆盖率：

| 文件 | 行覆盖率 |
| --- | ---: |
| `FundModels.swift` | 93.58% |
| `FundPortfolioOperations.swift` | 100.00% |
| `FundService.swift` | 99.34% |
| `FundStorage.swift` | 98.86% |
| `MarketModels.swift` | 100.00% |
| `MarketService.swift` | 100.00% |
| **核心代码合计** | **98.44%（567/576 行）** |

`AppRuntime.swift` 和 `MarketView.swift` 的交互路径由真实 App UI 验收覆盖，不计入上述
核心代码覆盖率。

## 4. 构建与接口

| 检查 | 命令或证据 | 结果 |
| --- | --- | --- |
| Release 编译 | `swift build -c release` | 通过 |
| App 打包 | `./build.sh` | 通过 |
| Plist | `plutil -lint GoldPrice.app/Contents/Info.plist` | 通过 |
| 严格验签 | `codesign --verify --deep --strict --verbose=2 GoldPrice.app` | 通过 |
| Agent 模式 | `LSUIElement=true` | 通过 |
| AU9999 | 返回 `f43/f59/f60/f169/f170` 完整字段 | 通过 |
| 上证指数 | 返回 `f43/f59/f60/f169/f170` 完整字段 | 通过 |
| 基金估值 | 4 个默认基金返回有效 JSONP | 通过 |
| 无估值降级 | `027300` 返回 `jsonpgz();`，界面显示 `--` | 通过 |

## 5. UI 全功能验收

所有会修改持仓的测试均在临时数据目录执行。

| 用例 | 验收结果 | 结果 |
| --- | --- | --- |
| 启动与刷新 | 启动后显示 AU9999、上证和基金今日估算 | 通过 |
| 悬浮窗口 | 系统窗口层级为 floating，Info.plist 不显示 Dock 图标 | 通过 |
| 展开/收起 | 320x56 与基金列表尺寸间正确切换 | 通过 |
| 窗口拖动 | 系统坐标从 `(-336,-915)` 变为 `(-436,-845)` | 通过 |
| 拖动后点击 | 拖动后展开按钮仍可正常点击 | 通过 |
| 行情链接 | 金价打开 AU9999 页面，上证打开指数页面 | 通过 |
| 基金链接 | `000001` 打开对应东方财富基金页面 | 通过 |
| 非法基金代码 | 保持弹窗并提示“基金代码必须是 6 位数字” | 通过 |
| 非法金额 | 保持弹窗并提示金额必须大于 0 | 通过 |
| 重复基金 | 保持弹窗并提示已存在 | 通过 |
| 添加基金 | 添加 `000001`，名称及估值自动更新 | 通过 |
| 加仓 | 本金从 100.00 更新为 150.00，份额同步增加 | 通过 |
| 减仓 | 本金从 150.00 更新为 120.00，份额按比例减少 | 通过 |
| 超额减仓 | 保持弹窗并提示超过当前持有市值 | 通过 |
| 无净值调仓 | `027300` 提示暂无可用净值 | 通过 |
| 重启持久化 | 重启后 `000001` 本金 120.00 和份额保持 | 通过 |
| 删除基金 | 删除后文件不再含 `000001`，窗口高度回退 | 通过 |
| 损坏恢复 | 原文件被备份，恢复 5 条默认持仓并显示文件名提示 | 通过 |
| 动态高度 | 5 条、6 条持仓分别显示 279 和 309 高度 | 通过 |
| 长列表滚动 | 60 条时高度封顶 1410，可滚动到第 60 条 | 通过 |
| 配色与占位 | 上涨红、下跌绿、平盘灰，无估值显示 `--` | 通过 |

## 6. 缺陷与修复闭环

| 缺陷 | 修复 | 回归结果 |
| --- | --- | --- |
| README 仍描述代码内固定持仓 | 按当前动态持仓、计算和持久化行为重写 | 通过 |
| `build.sh` 生成的 bundle 严格验签失败 | 最终 bundle 临时签名并内置 Plist/签名校验 | 通过 |
| 添加和调仓失败时静默关闭 | 抽取持仓业务规则并在弹窗显示明确原因 | 通过 |
| 存储路径不可隔离且错误被忽略 | 实例化存储、支持 QA 目录并显式返回错误 | 通过 |
| 损坏 JSON 被静默视为空持仓 | 先备份损坏文件，再恢复默认持仓并提示 | 通过 |
| 暂时无估值基金被误判为不存在 | 保留基金名称，显示 `--` | 通过 |
| 行情解析接受非正当前价 | 增加当前价必须大于 0 的校验 | 通过 |
| 声明支持拖动但实际窗口不移动 | 增加 AppKit `NSPanGestureRecognizer` 更新窗口坐标 | 通过 |

## 7. 最终状态

- README 与当前实现一致。
- `GoldPrice.app` 已重新构建并通过严格验签。
- 最终生产实例正在运行，未携带 QA 环境变量。
- 用户真实持仓文件未发生变化。
- 未新增第三方依赖，未提交或推送 Git 变更。
